{
  "name": "vr-exhibits",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/fetch-content.mjs && vite build && vite build --ssr src/entry-server.jsx --outDir dist-ssr && node scripts/prerender.mjs",
    "build:demo": "vite build --mode demo && vite build --mode demo --ssr src/entry-server.jsx --outDir dist-ssr && node scripts/prerender.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "@netlify/blobs": "^8.2.0",
    "preact": "^10.29.8"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.49",
    "preact-render-to-string": "^6.7.0",
    "tailwindcss": "^3.4.17",
    "vite": "^5.4.11"
  }
}
